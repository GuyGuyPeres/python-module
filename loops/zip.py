# hosts = ["host1", "host2", "host3"]
# ips = ["10.0.0.1", "10.0.0.2", "10.0.0.3"]
# for full_ip_adress in zip(hosts, ips):
#     print(full_ip_adress)
    
# users = ["alice", "bob", "charlie"]
# ids = [101, 102, 103]
# for user, id in zip(users, ids):
#     print(f"User {user} has the ID {id}")

# containers = ["c1", "c2", "c3"]
# statuses = ["running", "stopped", "paused"]
# for container, status in zip(containers, statuses):
#     if(status == "running"):
#         print(container)

# files = ["file1", "file2"]
# sizes = [100, 200]
# types = ["txt", "log"]
# for full_file in zip(files, sizes, types):
#     print(full_file)

# dbs = ["mysql", "postgres", "mongo"]
# ports = [3306, 5432, 27017]
# hosts = ["db1", "db2", "db3"]
# for db, port, host in zip(dbs, ports, hosts):
#     print(f"{db}-{port}-{host}")

# users = ["admin", "dev"]
# roles = ["full-access", "read-only"]
# regions = ["us", "eu"]
# for user, role, region in zip(users, roles, regions):
#     print(user, role, region)